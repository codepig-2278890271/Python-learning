"""
============================================================
 🐍 Python 零基础入门 —— 06：字符串操作
============================================================
 学习顺序：这是第 6 个文件（前提：已完成 05_循环与控制流.py）
 下一个文件：07_列表元组字典集合.py

 本节学习目标：
   ✅ 掌握字符串的常用方法（查找、判断、大小写转换等）
   ✅ 学会分割和拼接字符串
   ✅ 掌握 f-string 格式化输出
============================================================
"""

# ============================================================
# 字符串操作 —— 玩转文字
# ============================================================
print("\n" + "=" * 60)
print("字符串常用操作")
print("=" * 60)

# 字符串是 Python 中最常用的类型之一
s = "  Hello, Python World!  "
print("原始字符串:", repr(s))  # repr() 可以看到空格和真实内容

# 常用方法（方法就是"属于字符串的函数"，用 . 来调用）
print("\n--- 查找和判断 ---")
print("长度 len(s):         ", len(s))                    # 字符串长度
print("'Python' 在里面吗？   ", "Python" in s)             # 是否包含

# ------------------------------------------------------------
# 【链式调用详解】下面是 s.strip().startswith("Hel") 的逐层拆解：
#
#   s.strip() .startswith("Hel")
#   └─────┘  └──────────────────┘
#      ↓              ↓
#   第1步：        第2步：
#   去掉s首尾      判断结果是否以"Hel"开头
#   的空格         返回 True 或 False
#
#   过程：
#   s                              → "  Hello, Python World!  "  ← 开头有空格
#   s.strip()                      → "Hello, Python World!"      ← 空格没了
#   s.strip().startswith("Hel")    → True                        ← 以 Hel 开头！
#
#   . 就是"的" / "对…做…"：s.strip() → 对s做strip操作
#   链式调用 = 从左到右，上一步的结果传给下一步继续处理
#
#   为什么要 strip()？因为 s 开头是空格，直接 .startswith("Hel") 会返回 False
# ------------------------------------------------------------
print("以 'Hel' 开头？       ", s.strip().startswith("Hel"))  # 链式调用：先去空格，再判断开头
print("以 'ld!' 结尾？       ", s.strip().endswith("ld!"))    # 链式调用：先去空格，再判断结尾

print("\n--- 大小写转换 ---")
print("全部大写 s.upper():   ", s.upper())                 # 全大写
print("全部小写 s.lower():   ", s.lower())                 # 全小写
print("首字母大写 s.title():  ", s.title())                # 每个单词首字母大写

print("\n--- 清理和替换 ---")
print("去首尾空格 s.strip(): ", s.strip())                  # 去掉首尾空白
print("替换 s.replace():     ", s.replace("World", "中国")) # 替换文字

print("\n--- 分割和拼接 ---")
fruits = "苹果,香蕉,橘子,葡萄"
print("原文本:", fruits)
fruit_list = fruits.split(",")           # 用逗号分割成列表
print("分割后:", fruit_list)
print("拼接回去:", " + ".join(fruit_list))    # 用 " + " 把列表拼成字符串

# ----- f-string：格式化字符串（非常好用！推荐！）-----
print("\n--- f-string 格式化 ---")
name = "小明"
age = 25
height = 175.5

# 在字符串前面加 f，然后在 {} 里直接写变量名
print(f"我叫{name}，今年{age}岁，身高{height}厘米。")   # f-string：变量嵌入字符串，无多余空格
# 对比：print() 用逗号隔开多个参数时，默认会在参数之间加空格（sep=" "）
print("我叫",name,"今年",age,"岁，身高",height,"厘米。")  # 多参数：每个参数间自动加空格
# 输出对比：
#   f-string → 我叫小明，今年25岁，身高175.5厘米。  （紧凑，自己控制格式）
#   多参数   → 我叫 小明 今年 25 岁，身高 175.5 厘米。（参数间自动空格）

# {} 里面还能做运算
price = 9.9
quantity = 3
print(f"{quantity} 个苹果，每个 {price} 元，一共 {price * quantity} 元")

# 控制小数位数
pi = 3.141592653589793
print(f"π 保留两位小数：{pi:.2f}")
print(f"π 保留四位小数：{pi:.4f}")

print("\n" + "=" * 60)
print("✅ 本文件学习完成！接下来请打开：07_列表元组字典集合.py")
print("=" * 60)
